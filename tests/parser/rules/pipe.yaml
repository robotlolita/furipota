f "hello" |> g _
---
result: OK
rules: [Pipe, expression]
ast:
  !Pipe
  input:
    !Invoke
    callee: !Variable { id: !Identifier f }
    input: !Text "hello"
    options: !Record []
  transformation: 
    !Partial
    callee: !Variable { id: !Identifier g }
    options: !Record []